import React, { Component } from 'react';
import { View } from './error';

import './error.scss';

export default class Error extends Component {
    render() {
        return (
            <View />
        )
    }
}